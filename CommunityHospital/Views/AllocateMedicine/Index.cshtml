@model IEnumerable<CommunityHospital.Models.AllocateMedicine>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AMedicine.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AServiceCenter.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DistrictThanaRelationshipId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AMedicine.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AServiceCenter.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DistrictThanaRelationshipId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AllocateMedicineId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AllocateMedicineId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AllocateMedicineId })
        </td>
    </tr>
}

</table>
